sudo apt install git 
sudo apt install pandoc
sudo apt install libcurl4-openssl-dev   # rpm: libcurl-devel (Fedora, CentOS, RHEL)
sudo apt install libssl-dev             # rpm: openssl-devel (Fedora, CentOS, RHEL)
sudo apt install libxml2-dev            # rpm: libxml2-devel (Fedora, CentOS, RHEL)
sudo apt install libcairo2-dev          # rpm: cairo-devel (Fedora, CentOS, RHEL)

